<html>
  <head><meta charset="utf-8" /><title>Home - Productos</title></head>
  <body>
    <h1>Lista de Productos</h1>
    <ul>
      {{#each products}}
        <li>{{this.id}} - {{this.title}} - ${{this.price}}</li>
      {{/each}}
    </ul>
    <a href="/realtimeproducts">Ver productos en tiempo real</a>
  </body>
</html>